# 문의 폼 설정 방법

## 1. 생성된 Google Sheet

문의 접수 시트:

https://docs.google.com/spreadsheets/d/1c9ky0lA_yZQTa7RCMwRm2h4MR4TXSuZqkDK6tTVVZQ8/edit

## 2. Apps Script 만들기

1. Google Drive에서 새 Apps Script 프로젝트를 만듭니다.
2. `apps-script-inquiry.gs` 파일 내용을 그대로 붙여 넣습니다.
3. 저장합니다.
4. `배포 > 새 배포`를 누릅니다.
5. 유형은 `웹 앱`을 선택합니다.
6. 실행 사용자는 `나`로 둡니다.
7. 액세스 권한은 `모든 사용자`로 설정합니다.
8. 권한 승인을 완료합니다.
9. 생성된 Web App URL을 복사합니다.

## 3. 랜딩페이지에 Web App URL 넣기

`script.js`에서 아래 줄을 찾습니다.

```js
const INQUIRY_ENDPOINT = "";
```

따옴표 안에 복사한 Web App URL을 넣습니다.

```js
const INQUIRY_ENDPOINT = "https://script.google.com/macros/s/...../exec";
```

이후 GitHub에 다시 push하면 문의 폼 제출 시 Google Sheet에 저장되고 Gmail 알림이 발송됩니다.

## 4. 현재 상태

아직 Web App URL이 비어 있으면, 문의 폼은 이메일 앱을 열어 대체 접수하도록 동작합니다.
